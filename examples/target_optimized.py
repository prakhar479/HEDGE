def bubble_sort(arr):
    # For a truly "Pythonic" and idiomatic approach to sorting,
    # one should use Python's highly optimized built-in sort functions.
    # The 'sorted()' function returns a new sorted list.
    # If in-place sorting is desired, use arr.sort().
    return sorted(arr)